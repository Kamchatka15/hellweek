> Governed by `docs/ROBLOX_SUCCESS_LOGIC.md` (standing orders).
> Doctrine check (answer before coding): does this module touch first session, D1, stability, or the one daily reason to return? If no — name why it is being built now.

## Module: <Name>
Path: src/server/<Name>/init.luau
Owns: <single responsibility>
Does not own: <explicit exclusions>
Public API: <functions/events with types>
DataStore keys: <keys + schema version>
Threat model: <what the client must never be able to do>
Acceptance tests: <observable pass criteria>
Out of scope: <deferred>
