# QUEUE — what Claude does when Justin is away

> Read by any unattended session, including the nightly scheduled run. **Top first.** Only reversible work (`OPERATING_MANUAL.md` §8). Never Studio, never publish, never spend.
> Every unattended session: do the top item(s), tick them, append to `evidence/CHANGELOG.md`, and rewrite the "next attended move" line in `STATE.md`.

## Next attended move (rewrite this every unattended session)
**Justin's three clicks** — publish unlisted + Studio API access on (`docs/runs/PERSISTENCE_TEST.md`). Everything in Wave 4 is prepared and blocked on it.

## Now
- [ ] Draft the Wave-4 wiring as a diff-ready patch (theme.md names, shelf IDs, flood-timer config, lighting numbers) so the moment the place is published it applies in one step
- [ ] `lighting.md` for Fat Man Gets Rich: Future lighting, ClockTime, Atmosphere, ColorCorrection, Bloom on gold only, warm PointLight on the bank
- [ ] Refresh the three-comp CCU board (numbers only, ~20 min) — Mine a Mountain, Bee Swarm, Grow a Garden
- [ ] Re-run Candidate C through `tools/econ_sim.py`, one page, ready for G2
- [ ] Hunter's Gate A script printed to `games/fat-man-gets-rich/gate-a-script.md`

## Next
- [ ] Two more teardowns (comps 4–5) if the shelf or wedge look thin
- [ ] Icon + thumbnail prompts written from the formula (generation waits for G4)
- [ ] TestEZ installed + economy invariant tests
- [ ] `src/core/client/` mount so UIKit stops living in the game's client folder

## Standing (any spare unattended cycle)
- [ ] Keep `research/patterns/` growing — one teardown per spare cycle, any genre
- [ ] Re-verify a platform fact older than 2 months and file the result
- [ ] Read the ledger for anything at n=1 that a second title could reproduce

## Done
<!-- move ticked items here with a date and the commit -->
