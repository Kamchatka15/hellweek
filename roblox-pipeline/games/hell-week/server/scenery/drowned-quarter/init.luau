-- scenery/drowned-quarter — GENERATED by tools/gen_biome.py (seed 330451). Do not edit by hand.
--
-- 1175 parts across 2 chunks. Teepee mouths are exported so the loop can put a
-- chest in each one — the scenery says where a thing IS, the loop decides what
-- is in it and when it comes back.

local scenery = {}
for _, chunk in { script.chunk1, script.chunk2 } do
	for _, spec in require(chunk) do
		table.insert(scenery, spec)
	end
end

return {
	scenery = scenery,
	teepees = {
		Vector3.new(-236.7, 0, 120.2),
		Vector3.new(-169.9, 0, -69.7),
		Vector3.new(234.2, 0, -19.4),
		Vector3.new(41.8, 0, -101.8),
		Vector3.new(-258.5, 0, -60.2),
		Vector3.new(123.8, 0, -213.4),
	},
}
