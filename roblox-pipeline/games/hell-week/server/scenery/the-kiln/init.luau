-- scenery/the-kiln — GENERATED by tools/gen_biome.py (seed 910077). Do not edit by hand.
--
-- 972 parts across 2 chunks. Teepee mouths are exported so the loop can put a
-- chest in each one — the scenery says where a thing IS, the loop decides what
-- is in it and when it comes back.

local scenery = {}
for _, chunk in { script.chunk1, script.chunk2 } do
	for _, spec in require(chunk) do
		table.insert(scenery, spec)
	end
end

return {
	scenery = scenery,
	teepees = {
		Vector3.new(236.8, 0, -42.8),
		Vector3.new(200.1, 0, -45.0),
		Vector3.new(227.9, 0, -64.8),
		Vector3.new(49.7, 0, -212.6),
		Vector3.new(68.5, 0, -241.5),
	},
}
