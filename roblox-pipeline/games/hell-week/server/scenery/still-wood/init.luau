-- scenery/still-wood — GENERATED by tools/gen_biome.py (seed 771020). Do not edit by hand.
--
-- 2158 parts across 3 chunks. Teepee mouths are exported so the loop can put a
-- chest in each one — the scenery says where a thing IS, the loop decides what
-- is in it and when it comes back.

local scenery = {}
for _, chunk in { script.chunk1, script.chunk2, script.chunk3 } do
	for _, spec in require(chunk) do
		table.insert(scenery, spec)
	end
end

return {
	scenery = scenery,
	teepees = {
		Vector3.new(138.5, 0, -234.5),
		Vector3.new(-61.5, 0, 66.6),
		Vector3.new(-182.5, 0, 210.4),
		Vector3.new(-32.9, 0, -138.5),
		Vector3.new(-219.9, 0, -121.0),
		Vector3.new(122.8, 0, -88.8),
		Vector3.new(-121.8, 0, -71.3),
		Vector3.new(-246.0, 0, 37.3),
	},
}
