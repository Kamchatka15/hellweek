-- scenery/ashen-waste — GENERATED by tools/gen_biome.py (seed 20260912). Do not edit by hand.
--
-- 1459 parts across 2 chunks. Teepee mouths are exported so the loop can put a
-- chest in each one — the scenery says where a thing IS, the loop decides what
-- is in it and when it comes back.

local scenery = {}
for _, chunk in { script.chunk1, script.chunk2 } do
	for _, spec in require(chunk) do
		table.insert(scenery, spec)
	end
end

return {
	scenery = scenery,
	teepees = {
		Vector3.new(163.1, 0, -155.4),
		Vector3.new(169.4, 0, -190.7),
		Vector3.new(194.7, 0, -174.5),
		Vector3.new(205.8, 0, -116.7),
		Vector3.new(196.2, 0, -152.1),
		Vector3.new(222.3, 0, -139.7),
		Vector3.new(14.2, 0, -267.0),
		Vector3.new(23.2, 0, -239.7),
		Vector3.new(-2.5, 0, -245.3),
	},
}
