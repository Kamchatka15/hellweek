-- scenery/ashen-waste — GENERATED by tools/gen_biome.py (seed 20260912). Do not edit by hand.
--
-- 1462 parts across 2 chunks. Teepee mouths are exported so the loop can put a
-- chest in each one — the scenery says where a thing IS, the loop decides what
-- is in it and when it comes back.

local scenery = {}
for _, chunk in { script.chunk1, script.chunk2 } do
	for _, spec in require(chunk) do
		table.insert(scenery, spec)
	end
end

return {
	scenery = scenery,
	teepees = {
		Vector3.new(-23.5, 0, 142.5),
		Vector3.new(-105.5, 0, 122.3),
		Vector3.new(115.1, 0, 149.6),
		Vector3.new(114.7, 0, -206.0),
		Vector3.new(-12.3, 0, -134.4),
		Vector3.new(-169.1, 0, -186.0),
		Vector3.new(-125.0, 0, -87.0),
		Vector3.new(266.4, 0, -56.5),
		Vector3.new(189.7, 0, 5.2),
	},
}
