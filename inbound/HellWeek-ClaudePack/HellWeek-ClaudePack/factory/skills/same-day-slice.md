# Skill: same-day-slice

Trigger: "create a ___ game", "new title", "same-day", a name plus four paragraphs.

Do not ask the owner to fill a form. Draft `games/<slug>/brief.md` from the prose. Query the ledger. Start `comp-hunter`. Auto-cut with `cut-list`. Build on the engine. Dress from the Store shelf. You are not done until `studio-mcp` four-beat is green and persistence survives restart.

Grey box is not a deliverable. Listing is still a human G5 click. D1 is tomorrow's number.

If a title is already Active, log the new idea in IDEA_LOG.md and keep the slot.

See FACTORY_PLAN_v1.3_SAME_DAY.md.
