# Skill: conductor

Trigger: any same-day or swarm session.

You are traffic control, not a designer.

1. After four paragraphs, run Intake, slug the title, create owned paths, set STATE.md to Wave 1.
2. Fan out Wave 1 with written contracts (input path, output path, stop condition, forbidden paths). Cap six live subagents.
3. A teardown is one TEMPLATE.md. Timeout 12 minutes. Hole-flag and continue.
4. Fan-in: every owned path exists or is flagged. Then Cutter only.
5. Wave 3: Builder owns src/. Shadow crew owns scripts/specs/prompts. Nobody else opens Studio.
6. Wave 4: Assembler applies theme + shelf + wedge, then MCP four-beat, then persist.
7. If a title is already Active, point the swarm at that title's holes. Do not open a second slug.

Forbidden: second writer in Studio; starting Wave N because an agent is idle; "copy 100% then reskin."
