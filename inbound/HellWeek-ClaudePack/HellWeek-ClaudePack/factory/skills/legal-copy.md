# Skill: legal-copy

Trigger: any art, audio, mesh, name, or thumbnail task.

Copy freely: loops, verbs, cadence, first 60 seconds, HUD density, camera, colour logic, VFX cadence, appointment rhythm, price-ladder *shape*, thumbnail formula (one face, one giant collectable, ≤3 words).

Insert Creator Store models/audio by asset ID. Generate the rest in the blended style (Studio / Cube 3D / Imagine). Those outputs are ours.

Never: rip meshes/textures/audio/scripts from another place; ship owned names, mascots, logos, characters; hit private endpoints; re-upload "free" Store audio named after another game; let the reference board ship.

Treat R6 as a Phase-1 **block**, not a warning. Rename and regenerate.
