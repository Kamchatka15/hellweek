---
id: E-0007
claim: An engine built from one title is one-loop-shaped; the second title with a different verb forces the loader, the state packet, the funnel steps and the HUD to become loop-selectable, and none of that is visible until the second title arrives
domain: process
loop_verb: survive
session_shape: any
audience: any
monetization: any
wave: any
scale: pre-launch
outcome: observed
metric: Hell Week (survive) on the Fat Man Gets Rich (collect) engine — ContentLoader refused the pack outright; 4 existing modules needed a generic extension (StateSync.setBuilder, AnalyticsService.configure, EconomyService.drainCarry, PickupService.place) and 4 new ones were written (ClockService, BeaconService, StalkerService, SurviveLoop). ~1,300 new engine lines, 0 pack-specific lines in src/core (R7 clean)
confidence: n=1
observed: 2026-09-12
source: roblox-pipeline/docs/runs/2026-09-12-run-02.md
status: active
---
GAME_FACTORY §1 predicted this ("the day a new game forces an engine change, make it generic"). What it did not predict is the **size**: the sweep-and-bank loop had leaked into the loader's validation, the state packet, the funnel step names and the whole client HUD. Each of those is now a slot the pack fills. The third title is the test of whether the slots are right.

What would confirm this: a third title in a third verb family (build, race) landing with **no** new engine modules, only a new loop binder. What would refute it: the third title needing the loader changed again.
